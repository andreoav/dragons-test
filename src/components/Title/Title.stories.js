import React from 'react';
import { Title } from './Title';

export default {
  title: 'Components|Title',
  component: Title,
};

export const basicStyles = () => {
  return <Title>An awesome medieval heading</Title>;
};
